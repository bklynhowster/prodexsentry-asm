# Importers — read existing structured state (COMMANDsentry asset JSONs, etc.)
# and produce canonical entity records. Distinct from `parsers/` which read
# raw scan tool output and produce FindingEvents.
